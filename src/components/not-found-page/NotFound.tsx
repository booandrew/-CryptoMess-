import React from 'react';
import {Typography} from '../../uikit';

export const NotFound = () => {
    return (
        <Typography bold size="lg2">
            404 NotFound
        </Typography>
    );
};
